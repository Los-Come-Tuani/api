name: prek

on:
  push:
    branches-ignore:
      - "chore/deps"
      - "dependabot/**"
      - "**docs**"
  pull_request:
  workflow_dispatch:

concurrency:
  cancel-in-progress: true
  group: ${{ github.workflow }}-${{ github.ref }}

permissions:
  contents: read

jobs:
  run-prek:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: checkout repo
        uses: actions/checkout@v7

      - name: setup tools
        uses: ./.github/actions/setup-tools

      - name: execute
        run: |
          uv run  --frozen \
             prek --all-files \
                  --config .prek.toml \
                  --color always \
                  --show-diff-on-failure \
                  --skip just-pre-commit \
                  --skip no-commit-to-branch
